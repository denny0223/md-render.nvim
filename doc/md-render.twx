*md-render.twx*	Neovim 的 Markdown 渲染引擎（正體中文・台灣）

作者：delphinus　授權：MIT

English: `:help md-render-contents@en`
正體中文（台灣）: `:help md-render-contents@tw`
日本語: `:help md-render-contents@ja`

==============================================================================
目次                                                      *md-render-contents*

簡介                                                  |md-render-introduction|
系統需求                                              |md-render-requirements|
安裝                                                  |md-render-installation|
  Snacks 圖片後端                                     |md-render-snacks-image|
按鍵設定                                                  |md-render-mappings|
指令                                                      |md-render-commands|
  標題放大（實驗功能）                                   |md-render-text-size|
Telescope 整合                                           |md-render-telescope|
Snacks.nvim 整合                                            |md-render-snacks|
使用方式                                                     |md-render-usage|
  預覽                                                     |md-render-preview|
  函式庫 API                                                   |md-render-api|
  ContentBuilder                                   |md-render-content-builder|
  Markdown                                                |md-render-markdown|
  顯示工具                                           |md-render-display-utils|
  圖片                                                       |md-render-image|
  圖片分頁按鍵                                          |md-render-image-view|
範例                                                       |md-render-example|
緩衝區變數                                        |md-render-buffer-variables|
醒目提示群組                                            |md-render-highlights|
選用相依項目                                          |md-render-dependencies|

==============================================================================
簡介                                                  *md-render-introduction*

md-render.nvim 是 Neovim 的 Markdown 渲染引擎，能將 Markdown 原始文字轉為有
語法醒目提示、可互動的內容，直接顯示在編輯器中。支援浮動視窗、分頁、在同一個
視窗切換原始文字與預覽（|:MdRender-toggle|），以及從指令列啟動、類似 `less`
的分頁閱讀模式。

寫作時可用 |:MdRender-split| 將原始文字與即時預覽並排，兩側也會同步捲動。

主要功能：~

- 豐富的行內格式：粗體、刪除線、行內程式碼、連結、Obsidian `==highlight==`。
- 表格：框線、欄位對齊，以及依比例調整欄寬。
- 提示區塊與摺疊：支援 GitHub / Obsidian 的提示類型、彩色框線與圖示，可用滑鼠
  點擊或 `za` / `<CR>` 切換摺疊。
- 程式碼區塊：Treesitter 語法醒目提示；內容被截短時，可用滑鼠點擊或
  `za` / `<CR>` 展開。
- 圖片：透過終端機圖形協定在文件中顯示 PNG、JPEG、WebP、GIF 與 GIF 動畫。
- 影片：將 MP4、WebM、MOV、AVI、MKV、M4V 轉為動畫影格，在文件中播放。
- Mermaid 圖表：轉為圖片，直接顯示在文件中。
- PlantUML 圖表：透過本機工具或你指定的伺服器轉為圖片，顯示在文件中。
- CommonMark 段落：將原始文字中屬於同一段落的換行合併，包括清單項目的接續行
  與引用內容；中日韓字元之間不會插入多餘空白。
- 中日韓文字與行內標記之間的空白：例如 `これは **強調** です。` 中，僅為讓
  剖析器辨識 `**` 而加入的空白會收起；若相鄰字元是半形，則會保留空白，例如
  `これは **API** です。`。
- 巢狀區塊：縮排至清單項目內容位置的引用、提示與程式碼區塊會正常渲染。
- 中日韓文字換行：JIS X 4051 行首行尾禁則，加上選用的 BudouX 詞組切分。
- 可點擊的連結：滑鼠點擊開啟網址；游標懸停時，以小型浮動視窗顯示完整網址；
  也支援 OSC 8 超連結。
- `<details>`：可摺疊區塊，使用滑鼠點擊或 `za` / `<CR>` 切換。
- 函式庫 API：其他外掛可透過程式呼叫渲染引擎。

==============================================================================
系統需求                                              *md-render-requirements*

- Neovim >= 0.12（使用 |nvim_ui_send()| 將內容寫入終端機）。
- 若使用預設原生後端（"kitty"）在文件中顯示圖片與影片，需要支援 Kitty 圖形
  協定的終端機：
  - WezTerm（https://wezfurlong.org/wezterm/）
  - Kitty（https://sw.kovidgoyal.net/kitty/）
  - Ghostty（https://ghostty.org/）
- 選用的 Snacks 後端也需要 Unicode 佔位字元支援。本文的設定以 Kitty 單獨
  執行，或在 Kitty 中使用 tmux 為對象。
  相依項目與設定請見 |md-render-snacks-image|。

==============================================================================
安裝                                                  *md-render-installation*

基本範例使用預設原生後端（"kitty"）。若要在 Kitty/tmux 中顯示圖片、自動配合
視窗大小，以及縮放和平移，請接著閱讀 |md-render-snacks-image|。
請使用這個 fork 的預設分支；沿用自上游的發行標籤不包含這些新增功能，因此範例
使用 version = false。

使用 lazy.nvim： >lua

  {
    "denny0223/md-render.nvim",
    version = false,
    cmd = "MdRender",
    dependencies = {
      { "nvim-tree/nvim-web-devicons", version = "*" },
      { "delphinus/budoux.lua", version = "*" },
    },
    keys = {
      {
        "<leader>ms",
        "<cmd>botright vert MdRender split<CR>",
        desc = "在右側開啟 Markdown 預覽",
      },
      {
        "<leader>mp",
        "<Plug>(md-render-preview)",
        desc = "切換 Markdown 預覽",
      },
      {
        "<leader>mt",
        "<Plug>(md-render-preview-tab)",
        desc = "切換 Markdown 分頁預覽",
      },
      {
        "<leader>md",
        "<Plug>(md-render-demo)",
        desc = "Markdown 渲染示範",
      },
    },
  }
<

                                                      *md-render-snacks-image*
選用 Snacks 圖片後端 ~

若要使用靜態圖片、圖表、自動配合視窗大小與圖片分頁，請安裝：

- snacks.nvim：必須在開啟 md-render 預覽前載入並完成設定。
- 支援 Unicode 佔位字元的 Kitty。若使用 tmux，請在 tmux 設定中加入
  `set -g allow-passthrough on`，並重新載入設定。此整合尚未驗證其他 Snacks
  支援的終端機；原生後端的相容清單不能直接套用到此整合。
- ImageMagick：完整圖片流程需要 Neovim 的 PATH 能找到 `magick`。
  圖片分頁即使開啟 PNG 也需要此指令，無法用 ffmpeg、sips，或只有 convert
  指令的安裝取代。
- Mermaid：需要 mmdc（或 npx 替代方式）及其無頭瀏覽器。Snacks 負責顯示圖片，
  圖表仍由原本的工具產生，不會開啟瀏覽器視窗。npx 替代方式需要 Node.js/npm，
  且可能下載 CLI。其他選用功能請見 |md-render-dependencies|。

外掛管理器只會安裝 Neovim 外掛；指令列工具要另外安裝。
使用 lazy.nvim 時，請以以下設定取代上方的基本 md-render 設定；若有使用圖示或
BudouX，請保留其選用相依項目： >lua

  {
    "denny0223/md-render.nvim",
    version = false,
    cmd = "MdRender",
    dependencies = {
      {
        "folke/snacks.nvim",
        lazy = false,
        priority = 1000,
        opts = {
          image = {
            enabled = true,
            doc = { enabled = false },
            math = { enabled = false },
          },
        },
      },
    },
    config = function()
      require("md-render.image").setup({ backend = "snacks" })
    end,
    keys = {
      { "<leader>ms", "<cmd>botright vert MdRender split<CR>" },
      { "<leader>mp", "<Plug>(md-render-preview)" },
      { "<leader>mt", "<Plug>(md-render-preview-tab)" },
      { "<leader>md", "<Plug>(md-render-demo)" },
    },
  }
<
使用 vim.pack 時，將 "https://github.com/folke/snacks.nvim"
加入 vim.pack.add()。
使用 mini.deps 時，將 "folke/snacks.nvim" 加入 md-render 的 depends 清單。
兩個外掛都載入後，依照以下順序設定： >lua

  require("snacks").setup({
    image = {
      enabled = true,
      doc = { enabled = false },
      math = { enabled = false },
    },
  })
  require("md-render.image").setup({ backend = "snacks" })
<
如果已經設定過 Snacks，請把這些 image 選項合併至既有設定，不要呼叫兩次
setup()。停用 Snacks 的文件與數學式渲染，讓 md-render 負責預覽；此整合不需要
這兩項功能。

載入外掛後，執行 :checkhealth snacks，並檢查： >vim

  :echo executable('magick')
  :lua print(require("md-render.image").config().backend)
<
結果應分別為 1 與 "snacks"。在 tmux 中執行
`tmux show-options -gv allow-passthrough` 應顯示 "on"。最後，在 Kitty 中開啟
含有本機 PNG 的 Markdown 檔案，執行 :MdRender tab。等待圖片載入後，將游標
移到圖片或其標題，按 Enter 確認能開啟圖片分頁。只確認工具存在，還不足以驗證
實際顯示是否正常。

預設後端仍為 "kitty"。兩個後端都能播放 GIF 動畫與影片；Snacks 使用 Kitty
的動畫功能，也支援 tmux。影片影格擷取需要 ffmpeg。Snacks 會預先處理文件中
的所有圖片，包括畫面之外的圖片。圖表產生、下載與影格擷取共用最多兩個並行
工作，且此上限跨預覽共用；關閉預覽後，
已開始的轉換仍可能完成並寫入快取。因此，圖片較多的文件在開始時會做較多處理。
圖片配置請見 |md-render-image|，圖片分頁操作請見 |md-render-image-view|。

==============================================================================
按鍵設定                                                  *md-render-mappings*

本外掛提供 `<Plug>` 對應，但不設定任何預設快捷鍵。請自行指定： >lua

  vim.keymap.set("n", "<leader>ms",
    "<cmd>botright vert MdRender split<CR>")
  vim.keymap.set("n", "<leader>mp", "<Plug>(md-render-preview)")
  vim.keymap.set("n", "<leader>mt", "<Plug>(md-render-preview-tab)")
  vim.keymap.set("n", "<leader>md", "<Plug>(md-render-demo)")
<

範例中的 <leader>ms 請在 Markdown 原始文字視窗使用，每次都會在右側新增
預覽視窗。切換與關閉方式請見 |:MdRender-split|。

                                                   *<Plug>(md-render-preview)*
<Plug>(md-render-preview) ~
	切換目前 Markdown 緩衝區的浮動預覽視窗。若已開啟，則關閉預覽。

                                               *<Plug>(md-render-preview-tab)*
<Plug>(md-render-preview-tab) ~
	切換目前 Markdown 緩衝區的分頁預覽。若已開啟，則關閉預覽。

                                                    *<Plug>(md-render-toggle)*
<Plug>(md-render-toggle) ~
	在目前視窗切換 Markdown 原始文字與預覽。不開啟新視窗或分頁，而是替換
	視窗中的緩衝區。請見 |:MdRender-toggle|。

                                                      *<Plug>(md-render-auto)*
<Plug>(md-render-auto) ~
	[實驗功能] 切換目前緩衝區的自動模式：插入模式中顯示原始文字，其餘時間
	顯示預覽。請見 |:MdRender-auto|。

                                                     *<Plug>(md-render-split)*
<Plug>(md-render-split) ~
	上下分割視窗，同時顯示目前 Markdown 緩衝區的原始文字與預覽。
	請見 |:MdRender-split|。

                                                      *<Plug>(md-render-demo)*
<Plug>(md-render-demo) ~
	開啟示範視窗，展示所有支援的 Markdown 記法。

預覽視窗中可使用以下按鍵：

	`q` / `<Esc>` / `<C-c>`  關閉浮動／分頁預覽視窗。
	`za`                    切換游標所在的提示區塊摺疊或可展開區域；
	                        游標不在這些區域時，不執行任何動作。
	`<CR>`                  開啟游標所在的圖片（Snacks 後端），或切換
	                        提示區塊摺疊與可展開區域。
	`gf`                    跟隨本機檔案連結；Markdown 目標維持渲染。
	                        適用於 toggle、split、浮動及分頁預覽。
	`<LeftMouse>`           點擊連結、切換摺疊、展開區域。
	`<MouseMove>`           滑鼠懸停在連結上時，在編輯器底部的小型浮動
	                        視窗顯示完整網址。需要 'mousemoveevent'；
	                        md-render 會在預覽開啟期間自動啟用。

分割預覽請先切到預覽視窗，再用 `:q` 或 `<C-w>c` 關閉。

使用 |:MdRender-toggle| 開啟的預覽緩衝區不會把 `q` / `<Esc>` / `<C-c>` 綁定
為關閉操作；請再次執行 |:MdRender-toggle| 回到原始文字。`<LeftMouse>`、`za`
與 `<CR>` 仍可切換摺疊與展開區域；使用 Snacks 後端時，`<CR>` 也可開啟圖片。
`<LeftMouse>` 仍可開啟連結，`<MouseMove>` 仍可預覽完整網址。

`gf` 使用連結的確切目的地，依來源檔案目錄解析，不受工作目錄影響。
Markdown 目標沿用目前的預覽視窗；其他檔案在原始編輯視窗開啟，浮動／分頁
預覽會先關閉。`Ctrl-O` 返回前一份渲染文件，保留閱讀位置與摺疊狀態；從其他
檔案返回時，會在原始編輯視窗恢復渲染。游標不在連結上時，仍使用原生 `gf`，
也支援 `2gf` 等數字前綴。

目錄會在原始編輯視窗交給已設定的目錄瀏覽器（例如 netrw）開啟，浮動／
分頁預覽會先關閉。目錄畫面的導覽與緩衝區生命週期沿用該瀏覽器的行為。

`Ctrl-O` 與 `Ctrl-I` 維持 Neovim 原生行為。每個視窗各自保留 jumplist。
在來源編輯視窗開啟其他檔案後，只保證返回緊接在前的渲染文件；較早的
預覽歷史不會合併到該視窗。

支援相對路徑、POSIX 絕對路徑及本機 `file:///` URL，包含編碼檔名、行內／
參照連結及選用標題。檔案不存在或無法讀取時會留在原預覽。Fragment 目前
不會定位到標題；pager 導覽及 Windows／UNC 路徑不在首版支援範圍。

==============================================================================
指令                                                      *md-render-commands*

本外掛以單一使用者指令 |:MdRender| 搭配子指令提供功能。舊版的獨立指令
（`:MdRenderTab`、`:MdRenderToggle` 等）仍保留為已棄用的別名，請見
|md-render-deprecated-commands|。

                                                                   *:MdRender*
:MdRender [{subcommand}] [{args}] ~

	渲染目前的 Markdown 緩衝區。{subcommand} 決定顯示模式：

	  float       - 浮動預覽視窗（預設，等同單獨執行 |:MdRender|）。
	  tab         - 切換新分頁中的預覽。
	  pager       - 分頁閱讀模式（全螢幕，按 `q` 離開 Neovim）。
	  toggle      - 在目前視窗切換原始文字與預覽。
	  split       - 分割視窗，同時顯示原始文字與預覽；支援
	                `:vert`、`:tab`、`:topleft`、`:botright`。
	  auto        - [實驗功能] 隨插入模式自動切換；第二個引數接受
	                `on` / `off` / `toggle`（預設 `toggle`）。
	  textsize    - [實驗功能] 使用 Kitty 文字大小協定放大標題
	                （預設啟用）；第二個引數接受
	                `on` / `off` / `toggle`（預設 `toggle`）。
	  demo        - 開啟示範視窗，展示所有支援的記法。

	第一個引數可用 Tab 補齊子指令；在 `auto` 與 `textsize` 後，可補齊
	`on` / `off` / `toggle`。

	以下各節說明每個子指令的詳細行為。

                                                             *:MdRender-float*
:MdRender float ~
	切換目前 Markdown 緩衝區的浮動預覽視窗。
	未提供子指令時，|:MdRender| 預設使用此模式。

                                                               *:MdRender-tab*
:MdRender tab ~
	切換目前 Markdown 緩衝區的分頁預覽。

                                                            *:MdRender-toggle*
:MdRender toggle ~
	在目前視窗切換原始文字與預覽，不開啟新視窗或分頁。適合搭配分割視窗，
	例如在一側編輯程式碼，同時在另一側閱讀渲染後的 README。

	行為：

	- 預覽緩衝區為唯讀，切換時會重複使用；每個來源緩衝區各自快取一個。
	- 若多個視窗顯示同一份來源，只有執行指令的視窗會切換。其他視窗的編輯
	  會自動反映在預覽中。
	- 來回切換會對應游標位置：從預覽回到原始文字時，游標會移至最接近的
	  來源行；反向切換亦然。
	- 預覽視窗會停用 'number'、'relativenumber' 與 'list'。原始設定會保留
	  在視窗中，切回原始文字時還原。

                                                              *:MdRender-auto*
:MdRender auto [on|off|toggle] ~
	[實驗功能] 此功能仍在初期階段，操作方式可能調整。歡迎回報問題。

	依插入模式自動切換目前緩衝區的原始文字與預覽。狀態以緩衝區為單位，
	記錄在 `b:md_render_auto`。

	- `:MdRender auto on` 會立即切至預覽，並註冊 `InsertEnter` /
	  `InsertLeave` 自動指令，在進入插入模式時切回原始文字，離開時切回
	  預覽。
	- `:MdRender auto off` 會移除自動指令與緩衝區標記。若目前視窗正顯示
	  該緩衝區的預覽，也會切回原始文字，回到執行 `auto on` 前的狀態。
	- `:MdRender auto toggle` 或省略第二個引數的 `:MdRender auto` 會切換
	  啟用狀態。連續呼叫兩次會回到原始狀態（自動模式關閉、顯示原始文字）。

	外掛本身不會註冊 |FileType| 自動指令。若要為所有 Markdown 緩衝區啟用
	自動模式，請自行加入： >vim

	  autocmd FileType markdown silent! MdRender auto on
<
	行為：

	- 預覽緩衝區是 `nomodifiable`，無法直接觸發 |InsertEnter|。自動模式
	  啟用時，預覽緩衝區的 `i` / `I` / `a` / `A` / `o` / `O` 會先切回
	  原始文字，再於對應位置進入插入模式。
	- `<Esc>` 會回到來源的一般模式；|InsertLeave| 自動指令會等待 50 ms，
	  合併短時間內的重複事件後，再切回預覽。
	- 快速輸入 `i<Esc>i<Esc>` 時，會合併成最後的狀態。
	- 只切換目前視窗；其他顯示同一份來源的視窗需手動執行
	  |:MdRender-toggle|。

	預覽緩衝區可執行的操作：~

	  唯讀操作：移動（hjkl、gg、G 等）、搜尋（/、?）、複製（y）、視覺
	  選取、標記、`za` / `<CR>`（切換摺疊與展開區域），以及 <LeftMouse>
	  （摺疊、展開、開啟連結）。

	  `:w` 與 `:w!` 會轉交給來源緩衝區，因此不用離開預覽就能儲存。
	  不支援另存檔名（例如 `:w other.md`、`:saveas`）；請先用
	  |:MdRender-toggle| 切回原始文字。

	  預覽緩衝區命名為 `<source-path> [render]`，方便在狀態列與選擇器中
	  辨識。

	預覽緩衝區無法執行的操作：~

	  預覽緩衝區是 `nomodifiable`，因此修改內容的操作會被阻擋，或不產生
	  任何作用：

	  - 編輯按鍵：c C s S r R d D x X p P J ~ >> <<
	  - 復原／重做（u / <C-r>）：預覽緩衝區沒有自己的編輯紀錄。
	  - 修改緩衝區的 Ex 指令：:s、:!filter、gq。
	  - 包含編輯按鍵的巨集。

	  請先執行 |:MdRender-toggle| 切回原始文字；自動模式中也可直接按
	  `i` / `I` / `a` / `A` / `o` / `O`。

                                                             *:MdRender-split*
:MdRender split ~
	開啟分割視窗，同時顯示 Markdown 原始文字與預覽。若要一邊編輯、一邊
	在右側即時預覽，請從 Markdown 原始文字視窗執行： >vim

	  :botright vert MdRender split
<
	即使已有其他分割視窗，預覽仍會開在目前分頁的最右側。焦點會留在原始
	文字視窗，編輯後不必存檔就會更新預覽。一般模式下，<C-w>p（先按
	Ctrl-w 再按 p）會切到上一個視窗。剛開啟分割預覽後，可用它進入預覽。
	要關閉預覽，請在預覽視窗執行 `:q` 或按 `<C-w>c`；這個模式不使用
	單獨的 `q` 關閉。

	其他排列方式遵循 Vim 標準的分割修飾詞： >vim

	  :MdRender split          " 水平分割
	  :vert MdRender split     " 垂直分割
	  :tab MdRender split      " 在新分頁開啟另一種檢視
	  :topleft MdRender split  " 放在最上方
	  :botright MdRender split " 放在最下方
<
	行為是對稱的：

	- 從來源視窗執行時，新視窗顯示預覽。
	- 從由 |:MdRender-toggle| 或 |:MdRender-split| 建立的預覽視窗執行時，
	  新視窗顯示原始文字。

	每次執行都會新增視窗。

	預覽緩衝區與 |:MdRender-toggle| 共用工作階段快取，因此之後執行
	|:MdRender-toggle| 時會重複使用同一個預覽緩衝區。編輯後也會透過相同的
	即時更新流程，在 150 ms 的去彈跳等待後反映內容。

	來源與預覽視窗會同步游標及捲動位置。在任一側移動或捲動時，另一側會
	移至對應的內容位置。

	若游標已與另一側對應，就保持原位。多個來源行可能對應到同一個預覽行，
	例如收起的 <details>、表格或圖片；若直接反向對應，會把游標拉回區塊的
	第一行。視圖若距離對應位置僅一行，也會保持原位，避免另一側剛捲動一行
	就又被拉回。

	使用 `:q` 或 `<C-w>c` 關閉分割視窗不會銷毀預覽緩衝區；它會保留在來源
	的快取中，供之後切換或分割使用。

	限制：文件內的圖片一次只能綁定一個視窗。從來源連續執行
	|:MdRender-split| 兩次會產生兩個預覽視窗，但只有最新的視窗顯示圖片；
	較舊的視窗仍會即時更新文字內容。

                                                             *:MdRender-pager*
:MdRender pager ~
	分頁閱讀模式。以全螢幕顯示目前 Markdown 緩衝區的預覽，隱藏其他介面。
	按 `q` 離開 Neovim。適合像使用 `less` 一樣閱讀 Markdown： >bash

	  nvim +"MdRender pager" README.md
<
	也可以加入 shell 別名： >bash

	  alias mdless='nvim +"MdRender pager"'
	  mdless README.md
<

                                                          *:MdRender-textsize*
:MdRender textsize [on|off|toggle] ~
	[實驗功能] 使用 Kitty 文字大小協定，將標題繪製成比基本字型更大的尺寸。
	支援此協定的終端機預設會啟用；功能與限制請見 |md-render-text-size|。

	不帶引數時會切換啟用狀態。在不支援協定的終端機上啟用時，會顯示警告，
	並保持原狀。

	放大標題需要的額外行數是在建立內容時預留，因此切換此功能會重建畫面上
	所有預覽。

                                                              *:MdRender-demo*
:MdRender demo ~
	開啟示範視窗，展示所有支援的 Markdown 記法。

------------------------------------------------------------------------------
標題放大（實驗功能）                                     *md-render-text-size*

[實驗功能] 這是僅適用於 Kitty 的新功能。操作方式可能調整，也可能移除此功能。
歡迎回報問題。

Kitty 0.40 加入文字大小協定（OSC 66），可依基本字型的倍數繪製一段文字。
md-render 用它為各級標題設定不同大小：

	#	2.00x
	##	1.75x
	###	1.50x
	####	1.40x
	#####	1.25x
	######	1.17x

終端機支援時會自動啟用，不必另外設定。執行 `:MdRender textsize off` 可在本次
工作階段停用，或以下列設定永久停用： >lua

	require("md-render.text_size").setup { enabled = false }
<
各級尺寸是固定的。Kitty 的 `s=` 會放大文字佔用的格子數，而不只是字型，因此
每一級都維持 `s=2`，只多佔一行。小於 2 倍的尺寸則透過協定的分數比例
（`n=` / `d=`）與每段文字的寬度（`w=`）達成。協定將 `d` 上限設為 15，`w`
上限設為 7，因此最小標題為 1.17 倍，而非更接近原始大小的值。

運作方式：~

放大文字直接透過 `nvim_ui_send` 寫入終端機，使用與 |md-render-image| 的
Kitty 圖形相同的通道。Neovim 不知道這些額外繪製的內容，因此有以下處理：

- 緩衝區保留原始大小的標題，再把放大文字繪製在上面。終端機重繪時會暫時回到
  一般標題，不會變成空白；`y` / `/` / `:w` 仍可取得真正的文字。
- 放大標題會多預留一行，容納較高的字形，做法類似圖片預留顯示行數。
- 標題以一般寬度的 `1 / size` 換行，使各級標題都能放大，而非只有剛好放得下
  的標題。每個換行後的文字行各自佔用兩行高的區塊。
- 使用分數比例的標題會分成多段輸出。`n=` / `d=` 縮小字型時不會縮小格子，
  因此每段必須以 `w=` 指定寬度，而 `w` 最多為 7 格。
- 寬度必須是整數格，文字寬度卻可能不是，因此 `w` 一律無條件進位。若空間
  不足，Kitty 會捨棄超出的字元；進位的代價則只是段落右端多出約一格空間。
  文字會優先在寬度恰好吻合的位置分段，否則在空白後切開，讓剩餘空間呈現為
  稍寬的詞間距，而不是詞中間的缺口。等寬的純 ASCII 或純中日韓文字則總能
  恰好吻合，不需額外調整。
- Emoji 會獨立成段。對 Kitty 而言，設定 `w=` 的文字段是單一多格字元，整段
  只使用一種字型。若 emoji 與一般文字混在同一段，整段顯示可能失敗：在
  Kitty 0.48 的測試中，emoji 位於中間時，整段變成缺字方框；位於開頭時，
  後續文字會消失。獨立成段即可正常顯示，但寬度進位會讓 emoji 兩側間距稍大；
  若因此放不下，標題會維持一般大小。`#` 標題不受此限制，因為它只使用 `s=`
  縮放，文字段的 `w=0` 會讓 Kitty 逐格選擇字型。
- 每一級都預留兩行高，但只有 `#` 填滿整個區塊，因此其他級別以 `v=2` 垂直
  置中，而非使用協定預設的靠上對齊。若 `######` 的 1.17 倍文字靠上對齊，
  下方會留下幾乎一整行空白。
- 標題層級圖示維持一般大小，但會獨立成段繪製，讓它能在同一區塊內置中並與
  標題對齊。`s=2` 搭配 `n=1:d=2` 正好抵銷縮放。獨立成段也能避免裁切：
  Kitty 對每個來源格只提供 `s` 格空間，而 Nerd Font 圖示回報為一格寬，實際
  繪製卻更寬；與標題共用一段會讓 `󰉬` 被裁成只有 "H"。獨立使用 `w=1` 時，
  圖示可取得兩格寬，剛好容納原本的寬度。
- 捲動、調整視窗大小與移動游標都會觸發重繪，短時間內的事件會合併處理。
  持續捲動時會延長等待時間，讓按住 `<C-e>` 只需一次重繪，而非每一步都重繪。

限制：~

- 僅適用於 Kitty >= 0.40。外掛會透過 XTVERSION 查詢終端機身分，收到明確的
  支援回覆才會啟用；未回覆或不是 Kitty 時會停用。這是因為某些終端機會連同
  OSC 66 後的文字一起吞掉，導致標題消失，無法退回一般文字。在其他終端機上
  不會增加額外處理，標題維持原本的顯示方式。
- 放大時，標題內的行內程式碼、連結、`==highlight==` 等格式會失去個別顏色，
  整段統一使用標題的醒目提示。
- 若標題的第二行超出視窗範圍，會暫時維持一般大小，直到捲動至可完整顯示。
- 其他來源的重繪，例如外掛強制重繪、訊息或彈出視窗遮住預覽、滑鼠捲動造成
  終端機搬移格子，都會讓標題回到一般大小，無法完全避免。恢復時依訊號抵達
  速度處理：捲動、調整大小、開關視窗會立即重繪；裝飾提供者會在任何重繪後的
  下一輪事件迴圈補畫；其餘情況由有限速的 |SafeState| 處理，另有 500 ms
  計時器涵蓋一直未進入閒置狀態的情況。md-render 自己的重繪則會互相通知：
  圖片與放大標題都繪製在 Neovim 格線之外，也都會被全螢幕重繪清除，因此任一
  方重繪後會通知另一方立即補畫。
                                                       *md-render-eventignore*
- 若外掛在 'eventignore' `= "all"` 期間重繪，至少會有一個畫面影格無法立刻
  恢復。上述處理依賴自動指令得知事件，而此設定會停用所有自動指令。裝飾提供
  者仍會執行，因為它不是自動指令，所以標題會在下一輪事件迴圈恢復，但中間
  仍會短暫消失。
  已知案例是 nvim-scrollview（https://github.com/dstein64/nvim-scrollview）：
  它會開啟覆蓋整個編輯器的浮動視窗、移動十多個小視窗，再把它們關閉；滑鼠
  移動時每秒約執行二十次，全程都在 'eventignore' `= "all"` 之內。一次十五秒
  的量測中，308 次 `nvim_open_win` 呼叫只產生一次 |WinNew|。
  若要在預覽顯示期間停用它，可檢查 |b:md_render|，不必配對開啟與關閉事件。
  預覽可能重複開啟，也可能被手動分割；直接檢查是否有預覽顯示即可： >lua

    local off = false
    vim.api.nvim_create_autocmd({ "WinNew", "WinClosed", "BufWinEnter" }, {
      callback = vim.schedule_wrap(function()
        local want = false
        for _, w in ipairs(vim.api.nvim_list_wins()) do
          if vim.b[vim.api.nvim_win_get_buf(w)].md_render then
            want = true
            break
          end
        end
        if want ~= off then
          off = want
          vim.cmd(want and "ScrollViewDisable" or "ScrollViewEnable")
        end
      end),
    })
<
- 每次配置變更都需要全螢幕重繪以清除先前的放大文字，因此捲動成本較高。
  視窗中有圖片時，會利用圖片重繪清除畫面，再補上放大文字，避免重繪兩次。
- |md-render-telescope| 與 |md-render-snacks| 的預覽不啟用此功能。選擇器每次
  移動游標都會重繪，無法負擔每一步都做全螢幕重繪。

------------------------------------------------------------------------------
PlantUML 圖表                                             *md-render-plantuml*

標示為 `plantuml` 或 `puml` 的圍欄程式碼區塊會轉成圖片，與 `mermaid` 區塊
相同。外掛會使用 PATH 中的 `plantuml`；若 $PLANTUML_JAR 指向可讀取的 jar，
也可使用 `java -jar $PLANTUML_JAR`。

兩者都沒有時，內容會維持程式碼區塊。使用別人的 PlantUML 伺服器需要將圖表內容
傳送過去，因此外掛不會自行選擇遠端伺服器作為替代方式。指定伺服器後才會使用：
>lua

	require("md-render.image").setup {
	  plantuml_server = "https://plantuml.example.com/plantuml",
	}
<
若要使用公開服務，其網址為 "https://www.plantuml.com/plantuml"。無論指定哪個
伺服器，圖表原始碼都會傳送給它。

使用指定伺服器也需要 `curl`。產生的圖片以圖表原始碼為快取依據，存放在
`stdpath("cache")/md-render/plantuml`。同一份圖表在快取有效時只會傳送一次。

------------------------------------------------------------------------------
已棄用的指令                                   *md-render-deprecated-commands*

以下獨立指令仍保留為別名，以維持相容性。在每次 Neovim 工作階段中，各指令首次
執行時會透過 `vim.notify_once` 顯示警告，再轉交新的 |:MdRender| 指令處理。
未來的主要版本將移除這些別名。

                                                                *:MdRenderTab*
:MdRenderTab ~
	已棄用的 `:MdRender tab` 別名。

                                                             *:MdRenderToggle*
:MdRenderToggle ~
	已棄用的 `:MdRender toggle` 別名。

                                                               *:MdRenderAuto*
:MdRenderAuto [on|off|toggle] ~
	已棄用的 `:MdRender auto [on|off|toggle]` 別名。

                                                              *:MdRenderSplit*
:MdRenderSplit ~
	已棄用的 `:MdRender split` 別名。所有 Vim 分割修飾詞
	（`:vert`、`:tab`、`:topleft`、`:botright`）的作用相同。

                                                              *:MdRenderPager*
:MdRenderPager ~
	已棄用的 `:MdRender pager` 別名。

                                                               *:MdRenderDemo*
:MdRenderDemo ~
	已棄用的 `:MdRender demo` 別名。

==============================================================================
Telescope 整合                                           *md-render-telescope*

                                                       *telescope.previewer()*
預覽器 ~

telescope.previewer({opts}) ~
	建立可傳給 telescope.nvim 選擇器的預覽器，支援內建、擴充與自訂選擇器。

	參數：~
	  {opts}  (table|nil) 選用設定：
	    - {namespace}  (string) Extmark 命名空間名稱。
	                            預設為 "md_render_telescope"。

	回傳值：~
	  與 telescope 相容的緩衝區預覽器。

	範例： >lua
	  local previewer = require("md-render.telescope").previewer()
	  require("telescope.builtin").find_files({ previewer = previewer })
	  require("telescope").extensions.egrepify.egrepify({
	    previewer = previewer,
	  })
<

預覽器處理三類檔案：

  Markdown (.md, .markdown)   使用 md-render 顯示醒目提示、連結與文件內圖片。

  圖片／影片                  透過 Kitty 圖形協定顯示，標題列出檔名與尺寸。
  (PNG, JPEG, WebP, GIF,
  MP4, WebM, AVI, ...)

  其他檔案                    使用 telescope 預設預覽器及其語法醒目提示。

使用以 grep 為基礎的選擇器（例如 `live_grep`、`grep_string`）時，預覽會自動
捲動至符合的來源行。

                                                        *:Telescope_md_render*
:TELESCOPE MD_RENDER 擴充 ~

這是使用內建選擇器的捷徑，將 md-render 預覽器套用到 `telescope.builtin`
選擇器，所有引數都會原樣傳入。
>vim
  :Telescope md_render find_files
  :Telescope md_render live_grep cwd=~/notes
  :Telescope md_render grep_string search=TODO
<

==============================================================================
Snacks.nvim 整合                                            *md-render-snacks*

                                                            *snacks.preview()*
預覽 ~

snacks.preview({opts}) ~
	建立供 snacks.nvim 選擇器使用的預覽函式。處理與 Telescope 預覽器相同的
	三類檔案：Markdown 渲染、圖片／影片顯示，以及使用 Snacks 預設預覽器
	處理其他檔案。

	參數：~
	  {opts}  (table|nil) 選用設定：
	    - {namespace}  (string) Extmark 命名空間名稱。
	                            預設為 "md_render_snacks"。

	回傳值：~
	  與 snacks.nvim 相容的預覽函式。

	範例（全部來源）： >lua
	  require("snacks").setup({
	    picker = {
	      preview = require("md-render.snacks").preview(),
	    },
	  })
<

	範例（個別來源）： >lua
	  require("snacks").setup({
	    picker = {
	      sources = {
	        files = { preview = require("md-render.snacks").preview() },
	        grep = { preview = require("md-render.snacks").preview() },
	      },
	    },
	  })
<

預覽函式處理三類檔案：

  Markdown (.md, .markdown)   使用 md-render 顯示醒目提示、連結與文件內圖片。

  圖片／影片                  透過 Kitty 圖形協定顯示，標題列出檔名與尺寸。
  (PNG, JPEG, WebP, GIF,
  MP4, WebM, AVI, ...)

  其他檔案                    使用 Snacks 預設預覽器及其語法醒目提示。

使用以 grep 為基礎的選擇器時，預覽會自動捲動至符合的來源行。

==============================================================================
使用方式                                                     *md-render-usage*

------------------------------------------------------------------------------
預覽                                                       *md-render-preview*

                                                            *MdPreview.show()*
MdPreview.show({opts}) ~
	切換目前 Markdown 緩衝區的浮動預覽視窗。

	底部框線會顯示來源檔名、游標對應的來源行，以及框線字元組成的進度條： >
	  ╰──────────── README.md   25/100  ━━╾─────── ╯
<	這些資訊會隨游標移動更新；視窗太窄時，會先縮短進度條，再省略整個位置
	資訊。請見 |md-render-float-footer|。

	參數：~
	  {opts}  (table|nil) 選用設定：
	    - {max_width}  (integer) 最大渲染寬度。

	範例： >lua
	  require("md-render").preview.show({ max_width = 100 })
<

                                                        *MdPreview.show_tab()*
MdPreview.show_tab({opts}) ~
	切換目前 Markdown 緩衝區的分頁預覽。

	參數：~
	  {opts}  (table|nil) 選用設定：
	    - {max_width}  (integer) 最大渲染寬度。

	範例： >lua
	  require("md-render").preview.show_tab({ max_width = 100 })
<

                                                      *MdPreview.show_pager()*
MdPreview.show_pager({opts}) ~
	以分頁閱讀模式顯示目前 Markdown 緩衝區：全螢幕、最少介面，按 `q` 離開
	Neovim。

	參數：~
	  {opts}  (table|nil) 選用設定：
	    - {max_width}  (integer) 最大渲染寬度。

	範例： >lua
	  require("md-render").preview.show_pager()
<

                                                          *MdPreview.toggle()*
MdPreview.toggle({opts}) ~
	在目前視窗切換原始文字與預覽。請見 |:MdRender-toggle|。

	參數：~
	  {opts}  (table|nil) 選用設定，僅在首次建立預覽緩衝區時使用：
	    - {max_width}  (integer) 最大渲染寬度。

	範例： >lua
	  require("md-render").preview.toggle()
<

                                                         *MdPreview.auto_on()*
MdPreview.auto_on({opts}) ~
	[實驗功能] 為目前緩衝區啟用隨插入模式自動切換的功能，並立即切至預覽。
	請見 |:MdRender-auto|。

	參數：~
	  {opts}  (table|nil) 首次建立預覽緩衝區時傳給 |MdPreview.toggle()|。

                                                        *MdPreview.auto_off()*
MdPreview.auto_off() ~
	[實驗功能] 關閉目前緩衝區的自動切換。若目前視窗顯示此緩衝區的預覽，
	也會切回原始文字，使 `auto_off` 後的狀態與呼叫 |MdPreview.auto_on()|
	前一致。請見 |:MdRender-auto|。

                                                     *MdPreview.auto_toggle()*
MdPreview.auto_toggle({opts}) ~
	[實驗功能] 切換目前緩衝區的自動模式。請見 |:MdRender-auto|。

	參數：~
	  {opts}  (table|nil) 啟用時傳給 |MdPreview.auto_on()|。

                                                           *MdPreview.split()*
MdPreview.split({opts}) ~
	開啟分割視窗，同時顯示目前 Markdown 緩衝區的原始文字與預覽。
	請見 |:MdRender-split|。

	參數：~
	  {opts}  (table|nil) 選用設定：
	    - {mods}       (table) 指令修飾詞，通常是使用者指令回呼中的
	                           `args.smods`，例如 `{ vertical = true }`。
	    - {max_width}  (integer) 最大渲染寬度，僅在首次建立預覽緩衝區時
	                   使用。

	範例： >lua
	  require("md-render").preview.split({ mods = { vertical = true } })
<

                                                       *MdPreview.show_demo()*
MdPreview.show_demo() ~
	開啟示範視窗，展示所有支援的 Markdown 記法。

                                                   *MdPreview.build_content()*
MdPreview.build_content({lines}, {opts}) ~
	將 Markdown 文字行建立為渲染內容。此函式會擷取 YAML frontmatter，
	其餘交給 |ContentBuilder| 處理。

	參數：~
	  {lines}  (string[]) Markdown 原始文字行。
	  {opts}   (table) 傳給 |ContentBuilder:render_document()| 的選項。

	回傳值：~
	  內容結果表，請見 |ContentBuilder:result()|。

------------------------------------------------------------------------------
函式庫 API                                                     *md-render-api*

主模組 `require("md-render")` 會重新匯出以下子模組：

  `ContentBuilder`                                 |md-render-content-builder|
  `Markdown`                                              |md-render-markdown|
  `MarkdownTable`    表格剖析與渲染。
  `FloatWin`         浮動視窗管理。
  `TabWin`           分頁視窗管理。
  `display_utils`                                    |md-render-display-utils|
  `preview`          |md-render-preview|（延遲載入）。

------------------------------------------------------------------------------
ContentBuilder                                     *md-render-content-builder*

                                                        *ContentBuilder.new()*
ContentBuilder.new() ~
	建立新的 ContentBuilder 實例。

                                            *ContentBuilder:render_document()*
ContentBuilder:render_document({lines}, {opts}) ~
	渲染完整的 Markdown 文件。

	參數：~
	  {lines}  (string[]) Markdown 原始文字行。
	  {opts}   (table|nil) 選項：
	    - {max_width}           (integer) 最大寬度，預設 80。
	    - {indent}              (string) 行首縮排，預設 "  "。
	    - {max_lines}           (number) 最大行數，預設 math.huge。
	    - {repo_base_url}       (string) 相對連結的基底網址。
	    - {autolinks}           (table[]) 自動連結規則，每筆包含：
	                              - {key_prefix} (string)，例如 "JIRA-"。
	                              - {url_template} (string)，例如
	                                "https://...JIRA-<num>"。
	                              - {is_alphanumeric} (boolean|nil)。
	    - {fold_state}          (table) 提示區塊的摺疊狀態。
	    - {expand_state}        (table) 可展開區域的狀態。
	    - {source_line_offset}  (integer) 來源行偏移量。

                                                     *ContentBuilder:result()*
ContentBuilder:result() ~
	回傳渲染後的內容。結果表包含文字行、醒目提示、連結中繼資料、程式碼
	區塊、摺疊、可展開區域、圖片，以及 |display_utils| 使用的來源行對應。

                                                   *ContentBuilder:add_line()*
ContentBuilder:add_line({text}, {hl_groups}) ~
	新增單行文字，可指定醒目提示群組。

                                                *ContentBuilder:add_labeled()*
ContentBuilder:add_labeled({label}, {value}, {value_hl}) ~
	新增一行鍵值配對，用於渲染 frontmatter。

                                       *ContentBuilder:add_wrapped_markdown()*
ContentBuilder:add_wrapped_markdown({text}, ...) ~
	新增一行 Markdown，套用自動換行與行內格式。

	完整參數：~
	  {text}, {indent}, {max_width}, {repo_base_url}, {autolinks},
	  {ref_links}, {footnote_map}

                                        *ContentBuilder:add_simple_markdown()*
ContentBuilder:add_simple_markdown({text}, ...) ~
	新增一行 Markdown，套用行內格式但不自動換行。

	完整參數：~
	  {text}, {indent}, {repo_base_url}, {autolinks}, {ref_links},
	  {footnote_map}

                                                  *ContentBuilder:add_table()*
ContentBuilder:add_table({table_lines}, ...) ~
	渲染具有框線的 Markdown 表格。

	完整參數：~
	  {table_lines}, {indent}, {max_width}, {repo_base_url}, {autolinks}

------------------------------------------------------------------------------
Markdown                                                  *md-render-markdown*

                                                           *Markdown.render()*
Markdown.render({text}, ...) ~
	將單行 Markdown 渲染為顯示文字、醒目提示與連結中繼資料。

	參數：~
	  {text}           (string)     Markdown 原始文字。
	  {repo_base_url}  (string|nil) 相對連結的基底網址。
	  {autolinks}      (table[]|nil) 自動連結規則。
	  {ref_links}      (table|nil)  參照連結定義。
	  {footnote_map}   (table|nil)  註腳定義。

	回傳值：~
	  {text}        (string)  顯示文字。
	  {highlights}  (table[]) 醒目提示範圍。
	  {links}       (table[]) 連結中繼資料。

                                                     *Markdown.heading_slug()*
Markdown.heading_slug({text}) ~
	將標題文字轉換為與 GitHub 相容的 URL slug。

                                                  *Markdown.parse_footnotes()*
Markdown.parse_footnotes({lines}) ~
	從 Markdown 文字行剖析註腳定義。

                                            *Markdown.parse_reference_links()*
Markdown.parse_reference_links({lines}) ~
	從 Markdown 文字行剖析參照連結定義。

                                           *Markdown.renumber_ordered_lists()*
Markdown.renumber_ordered_lists({lines}) ~
	依 CommonMark 規則重新編號有序清單項目。

------------------------------------------------------------------------------
顯示工具                                             *md-render-display-utils*

                                     *display_utils.apply_content_to_buffer()*
display_utils.apply_content_to_buffer({buf}, {ns}, {content}, {opts}) ~
	將渲染結果（文字行、醒目提示、連結）套用至緩衝區。

                                 *display_utils.apply_treesitter_highlights()*
display_utils.apply_treesitter_highlights({buf}, {ns}, {content}) ~
	為程式碼區塊套用 Treesitter 語法醒目提示。

                                           *display_utils.open_float_window()*
display_utils.open_float_window({buf}, {content}, {float_win}, {opts}) ~
	為渲染內容建立浮動視窗並設定位置。

	參數：~
	  {opts}  (table|nil) 選用設定：
	    - {title}     (string) 框線標題。
	    - {position}  ("mouse"|"center") 顯示位置。
	    - {enter}     (boolean) 是否將焦點移至視窗。
	    - {footer}    (table[]) 底部框線的文字片段清單，通常由
	                  |display_utils.build_footer_chunks()| 建立。

                                         *display_utils.build_footer_chunks()*
                                                      *md-render-float-footer*
display_utils.build_footer_chunks({info}, {width}) ~
	建立浮動視窗底部狀態資訊所用的 `{text, hl_group}` 文字片段清單。

	狀態資訊放在底部框線，因為浮動視窗只有在 'laststatus' 為 1 或 2 時才
	會繪製狀態列。當 'laststatus' 為 3（lualine 的 `globalstatus` 設定）
	時，浮動視窗自己的 'statusline' 會改用於畫面底部的全域狀態列，既無法
	在浮動視窗顯示資訊，還會覆蓋原本的狀態列。底部框線則一律顯示，也不會
	佔用內容行。

	配置為 `<name>  <line>/<total>  <bar>`。進度條寬十格，以框線字元組成：
	`━` 表示已捲過的部分、`─` 表示其餘部分、`╾` 表示半格。未填滿的細線與
	框線本身使用相同字元，看起來會像框線的延伸。

	行號以 {total} 的寬度靠右對齊，因此捲動時整體寬度固定。若超出 {width}
	則進度條會縮短至最少四格，再從右側依序移除進度條、位置與檔名。寬度以
	顯示格數計算，因此也能正確處理中日韓檔名。

	參數：~
	  {info}   (table) 要顯示的資訊：
	    - {name}   (string|nil) 檔名。
	    - {line}   (integer|nil) 目前行號，不超過 {total}。
	    - {total}  (integer|nil) 總行數。
	  {width}  (integer) 浮動視窗內部寬度。

	回傳值：~
	  (table[]) 文字片段清單；空間不足以顯示任何資訊時為空。

                                            *display_utils.set_float_footer()*
display_utils.set_float_footer({win}, {chunks}, {pos}) ~
	將 |display_utils.build_footer_chunks()| 的底部資訊套用至浮動視窗。
	{pos} 預設為 `"right"`。視窗已關閉或不是浮動視窗時不執行任何動作，讓
	呼叫端可與分頁、分割及分頁閱讀模式共用程式碼。

                                         *display_utils.setup_float_keymaps()*
display_utils.setup_float_keymaps({buf}, {ns}, {win}, {content}, ...) ~
	在浮動視窗中設定互動按鍵，例如點擊連結、切換摺疊等。

	完整參數：~
	  {buf}, {ns}, {win}, {content}, {float_win}, {opts}

                                               *display_utils.supports_osc8()*
display_utils.supports_osc8() ~
	檢查終端機是否支援 OSC 8 超連結。

                                                *display_utils.setup_images()*
display_utils.setup_images({win}, {content}, {ns}, {opts}) ~
	初始化視窗中的圖片顯示。

	參數：~
	  {win}      (integer) 視窗代號。
	  {content}  (table) |ContentBuilder:result()| 回傳的內容。
	  {ns}       (integer|nil) Extmark 命名空間。
	  {opts}     (table|nil) 選用設定：
	    - {buf}            (integer) 用於自動重建的緩衝區代號。
	    - {build_content}  (function) 網路圖片下載後呼叫，以正確尺寸重建
	                       內容，並回傳新的內容表。

------------------------------------------------------------------------------
圖片                                                         *md-render-image*

Snacks 的設定、系統需求與檢查方式請見 |md-render-snacks-image|。

使用 Snacks 時，自動配置會採用視窗可用寬度，除非明確設定 max_width；圖片高度
則限制在視窗高度減去六行之內。圖片會維持長寬比，且不會放大至超過原始像素尺寸。

                                                        *md-render-image-view*
使用 Snacks 後端與 ImageMagick（magick）時，在圖片或其標題上按 <Enter> 可開啟
獨立圖片分頁。以下按鍵會自動設定：

  播放       <Space>；暫停／繼續 GIF 或影片。
  縮放       + / = / -；也支援數字鍵盤的 + / -。
  平移       h/j/k/l 或方向鍵；每次移動可見寬度／高度的 1/8。
  半頁       zH / zL（左／右）、<C-d> / <C-u>（下／上）。
  翻頁       <C-f> / <C-b> 或 <PageDown> / <PageUp>（下／上）。
  細移       <C-e> / <C-y>；向下／上移動一個畫面行。
  邊界       0 / ^ / <Home> 與 $ / <End>（左／右）；
             gg / <C-Home> 與 G / <C-End>（上／下）。
  完整圖片   f；將完整圖片調整至視窗內並置中。
  說明       ?；開啟本節操作說明。
  返回       q / <Esc>；關閉圖片分頁，回到文件。

GIF 與影片分頁需要 Kitty 0.31 以上版本，開啟後會自動播放；暫停時停在目前
影格，縮放與平移會維持播放狀態。
播放控制只影響此分頁，與文件預覽各自獨立。沿用文件內的無聲影格預覽；使用
ffmpeg 時為每秒 5 格、最大 400x400 像素與 300 格（最多 60 秒）。

數字前綴可重複平移、半頁、翻頁與細移的步數，例如 3j 或 2<PageDown>。翻頁時，
空間允許便保留兩個畫面行的重疊範圍。跳至邊界會保留縮放倍率與另一軸的位置。
數字鍵盤的導覽鍵也可使用。

啟用 'mouse' 後，滾輪向上／下可放大／縮小圖片，效果與 +/- 相同，不切換焦點。
按住滑鼠左鍵拖曳，圖片便會跟著指標移動；放開即停止。觸控板的垂直捲動事件也會
用於縮放。

縮放範圍為完整圖片適應視窗時的 1 至 16 倍，只會放大既有像素，不會重新產生圖表。

                                                     *image.set_download_fn()*
image.set_download_fn({fn}) ~
	設定自訂的網路圖片下載函式，預設使用 curl。

	參數：~
	  {fn}  (function) 函式簽章：
	          fn(url: string, dest: string, callback: function)

                                                         *image.reset_cache()*
image.reset_cache() ~
	清除所有圖片與 Mermaid 渲染快取。

                                                      *image.supports_kitty()*
image.supports_kitty() ~
	檢查終端機是否支援 Kitty 圖形協定。

==============================================================================
範例                                                       *md-render-example*

以下完整範例示範如何渲染 Markdown，並在緩衝區中顯示：
>lua

  local md = require("md-render")

  -- 渲染單行 Markdown
  local text, highlights, links =
    md.Markdown.render("**bold** and [link](https://example.com)")

  -- 建立完整文件內容
  local ContentBuilder = md.ContentBuilder
  local b = ContentBuilder.new()
  b:render_document(lines, {
    max_width = 80,
    indent = "  ",
    repo_base_url = "https://github.com/user/repo",
    autolinks = {
      {
        key_prefix = "JIRA-",
        url_template =
          "https://jira.example.com/browse/JIRA-<num>",
      },
    },
  })
  local content = b:result()

  -- 套用至緩衝區
  local buf = vim.api.nvim_create_buf(false, true)
  local ns = vim.api.nvim_create_namespace("my_ns")
  md.display_utils.apply_content_to_buffer(buf, ns, content)

  -- 顯示圖片（需要支援 Kitty 圖形協定的終端機）
  -- 視窗關閉時，圖片會自動清除。
  local win = vim.api.nvim_get_current_win()
  md.display_utils.setup_images(win, content, ns)
<

==============================================================================
緩衝區變數                                        *md-render-buffer-variables*

                                                       *md-render-b-md-render*
                                                                 *b:md_render*
b:md_render ~
	所有由 md-render 渲染的緩衝區都會設為 `true`，包括浮動預覽、分頁與分割
	視窗、分頁閱讀模式、|:MdRender-toggle| 切入的緩衝區，以及
	|:MdRender-demo|。其他緩衝區不會設定此值，因此可據此辨識預覽，不必依賴
	可能被使用者或其他外掛修改的緩衝區名稱或檔案類型。

	此標記刻意設在緩衝區，而非視窗。|:MdRender-toggle| 會保留視窗並替換
	其中的緩衝區；手動分割預覽也會產生外掛不知情的新視窗。標記必須隨著
	緩衝區，才能涵蓋兩種情況。若要判斷畫面上是否有預覽，請巡覽視窗： >lua

	  local function preview_visible()
	    for _, w in ipairs(vim.api.nvim_list_wins()) do
	      if vim.b[vim.api.nvim_win_get_buf(w)].md_render then return true end
	    end
	    return false
	  end
<
	應用範例請見 |md-render-eventignore|。

==============================================================================
醒目提示群組                                            *md-render-highlights*

所有醒目提示群組都以 `default = true` 定義，因此配色主題可以覆寫。

行內格式 ~

  *Bold*                粗體文字（備用設定：`bold = true`）。
  *Italic*              斜體文字（備用設定：`italic = true`）。
  *MdRenderInlineCode*  行內程式碼（`` `code` `` 與 `<code>`）。
                        前景色沿用 `String`；背景色將 Comment 前景色與
                        Normal 背景色混合成淡中性色，使程式碼與標題易於區分。
  *MdRenderHighlight*   Obsidian `==highlight==` 標記。
  *MdRenderTag*         Obsidian `#tag` 語法（連結至 `Label`）。
  *MdRenderMath*        行內／區塊數學式（連結至 `Special`）。
  *MdRenderLinkAnchor*  錨點連結 `#heading`（連結至 `DiagnosticHint`）。
                                                        *MdRenderLinkObsidian*
                        Obsidian wikilink `[[...]]`（連結至 `Special`）。

標題 ~

  *MdRenderH1*  第一級標題（來自 Treesitter 的標題醒目提示）。
  *MdRenderH2*  第二級標題。
  *MdRenderH3*  第三級標題。
  *MdRenderH4*  第四級標題。
  *MdRenderH5*  第五級標題。
  *MdRenderH6*  第六級標題。

警示／提示區塊 ~

各提示類型的 {Type} 可為 Note、Tip、Important、Warning、Caution、Abstract、
Todo、Success、Question、Failure、Danger、Bug、Example、Quote：

  *MdRenderAlert{Type}*   提示標題文字顏色，使用粗體，顏色取自對應的診斷群組。
  *MdRenderAlert{Type}Bg* 提示背景色，由提示前景色與 NormalFloat 背景色混合。

DETAILS 區塊 ~

  *MdRenderDetailsBg*   `<details>` 區塊背景色。
  *MdRenderDetailsBar*  `<details>` 區塊框線／指示符號。

圖片 ~

                                                    *MdRenderImagePlaceholder*
                        圖片載入期間的佔位提示。

浮動視窗底部資訊 ~

  *MdRenderFooter*      浮動預覽底部框線的狀態資訊（連結至 `FloatFooter`）。
  *MdRenderFooterName*  底部資訊的檔名部分（連結至 `FloatTitle`）。
  *MdRenderFooterBar*   進度條已填滿的部分（連結至 `FloatTitle`）。
                                                      *MdRenderFooterBarEmpty*
                        進度條未填滿的部分。連結至 `FloatBorder`，讓它與
                        所在框線融合。

分割模式 ~

                                                        *MdRenderShadowCursor*
                        |:MdRender-split| 中，未取得焦點那一側的游標位置
                        提示，標示目前游標對應的行（連結至 `CursorLine`）。

==============================================================================
選用相依項目                                          *md-render-dependencies*

基本 Markdown 渲染不需要以下項目，但使用對應功能時就是必要條件。指令列工具
要另外安裝，並且能從 Neovim 的 PATH 找到。

  snacks.nvim          選用圖片後端、自動配合視窗大小，以及圖片分頁。
                       設定請見 |md-render-snacks-image|。
                       https://github.com/folke/snacks.nvim

  nvim-web-devicons    程式碼區塊標題的檔案類型圖示。
  （或 |mini.icons|）  未安裝時使用內建圖示表。
                       https://github.com/nvim-tree/nvim-web-devicons
                       https://github.com/echasnovski/mini.icons

  budoux.lua           中日韓文字的詞組換行（BudouX）。未安裝時依字元斷行，
                       仍保留行首行尾禁則。
                       https://github.com/delphinus/budoux.lua

  Treesitter 剖析器    程式碼區塊的語法醒目提示。未安裝時仍會顯示程式碼，
                       但沒有語法醒目提示。

  curl                 下載網路圖片與影片。可透過 |image.set_download_fn()|
                       改用自訂下載函式。

  FFmpeg               原生後端的 JPEG/WebP 轉 PNG，以及兩個後端共用的
  (ffmpeg/ffprobe)     GIF 動畫／影片影格擷取。圖片可改用 ImageMagick，
                       影片仍需要 ffmpeg。

  ImageMagick          Snacks 轉換非 PNG 圖片時需要此工具。圖片分頁即使開啟
  (magick)             PNG 也需要 magick，不能以 ffmpeg、sips 或只有 convert
                       的安裝取代。
                       原生靜態圖片轉換依序嘗試 sips、ffmpeg、magick；
                       兩個後端的 GIF 影格擷取依序嘗試 ffmpeg、magick。
                       Snacks 靜態圖片轉換使用 ImageMagick。
                       https://github.com/folke/snacks.nvim/blob/main/docs/image.md

  Mermaid CLI          將 Mermaid 圖表轉為圖片。找不到 mmdc 時改用
  (mmdc)               `npx -y @mermaid-js/mermaid-cli`，需要 Node.js/npm，
                       且可能下載 CLI。兩種後端都仍需要其無頭瀏覽器。
                       https://github.com/mermaid-js/mermaid-cli

  PlantUML             將 PlantUML 圖表轉為圖片。只有在你指定伺服器時，
  （plantuml，或 java  才會改用該伺服器；請見 |md-render-plantuml|。
  搭配 $PLANTUML_JAR） https://plantuml.com/

==============================================================================
vim:tw=78:ts=8:ft=help:norl:
