# 指南 B

你已透過 `gf` 開啟另一份 Markdown。這裡應保持渲染，沿用剛才的預覽視窗。

[繼續到終點 C](end.md)

這個目的地只有 `end.md`，會依本檔案所在的 `docs` 目錄解析。

[重新造訪首頁 A](../README.md)

[直接開啟一般筆記](../notes.txt)

> [!TIP]- 可保留的閱讀狀態
> 在標題上按 `za` 展開，前往 C，再按 `Ctrl-O` 回來，檢查展開狀態。

要驗證真正的返回歷史，請使用 `Ctrl-O`。跟隨「重新造訪首頁 A」會新增一次導覽紀錄，可用來測試循環造訪。
